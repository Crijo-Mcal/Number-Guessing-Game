A CLI-based Number Guessing Game developed using TypeScript and Node.js.  
Players select a difficulty level (Easy, Medium, Hard), which determines the number of attempts.  
The goal is to guess a random number between 1 and 100, with hints provided after each guess.

url project : https://roadmap.sh/projects/number-guessing-game
